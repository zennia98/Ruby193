        <section class="page">
            <div class="page__inner">

                <div class="page__text{% if settings.use_footer %} hasfooter{% endif %} rte">
                	<h1>{{ page.title }}</h1>
                    {{ page.content }}
                </div>

            </div>
        </section>